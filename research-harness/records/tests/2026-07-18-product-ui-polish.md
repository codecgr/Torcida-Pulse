# Product/UI polish evidence — 2026-07-18 17:37 BRT

Branch: `codex/txodds-release`. This checkpoint is intentionally uncommitted so
the user can test before any commit or merge.

## Facts

- The 375×812 picker keeps the promise, selected match, and primary CTA in the
  first viewport. No loading contract, API contract, replay normalization, or
  provenance state transition was changed.
- Mobile replay controls are fixed above the safe-area inset while replay is in
  progress and return to normal document flow at completion. Interactive
  targets remain at least 44 px.
- Auto-pause now presents an editorial `A virada aconteceu` moment with minute,
  score transition, same-tuple TxLINE signal movement, an explicit rarity
  explanation, fan actions, and truthful provenance copy. Technical proof
  details remain behind `Ver prova` and retain their original state-dependent
  verified/unavailable/failed/synthetic behavior.
- The ending can save a fan preference locally. The stored JSON contains only
  `saved` and `savedAt`; it contains no fixture, team, score, event, odds,
  endpoint, proof, PDA, token, or credential data. The visual card continues to
  state that it is a concept without mint.
- `prefers-reduced-motion` disables the new smooth proof navigation, in addition
  to the existing CSS motion reduction.
- Official read-only recheck immediately before this checkpoint still rendered
  Brasil and Consumer as `Open` / `Submit Now`, with 5 and 97 submissions,
  respectively; the global event rendered `Submissions open`. No login, submit,
  push, deployment, publication, contact, or spend occurred.

## Reproducible validation

Command:

```bash
npm run verify
```

Result:

- Vitest: 12 files, 68/68 tests passed.
- Replay manifest and double-submission packet consistency: passed.
- Playwright: 48/48 passed in one Chromium worker.
- Responsive/language/state matrix: 320, 375, and 1280 px; PT-BR and EN;
  picker, initial, auto-pause, final, and error states; full axe scan and
  horizontal-overflow checks passed.
- Keyboard/focus, fixed mobile controls, first-fold CTA, loading fallback,
  truthful proof expansion, local-memory minimization, error, restart, and
  final continuity checks passed.
- Security audit and production local smoke: passed.
- `git diff --check`: passed.

Build hashes:

| Artifact | SHA-256 |
| --- | --- |
| `dist/index.html` | `ef541c741739b23e8277fd8123d7ed702bfb7d5a46cba1777bc5d3078af58e79` |
| `dist/assets/index-Cz17AveA.css` | `7ff6c2dab16e83b40bf7ddf7d9351fe6734fe08487e72bd98fd69563429237dd` |
| `dist/assets/index-QrexhMhm.js` | `72277ec1d95a22e319b27848f5bd871fbc1cb699c373f39e6d19363ce53b5071` |

Visual evidence generated by Playwright (ignored test output):

- `test-results/e2e-turning-point-375.png`
- `test-results/e2e-ending-375.png`

## Inferences

- The product now expresses the intended emotion → evidence → memory sequence
  more directly without adding a livescore, betting surface, mint, rewards,
  signup, or nonexistent backend.
- Keeping the original picker structure was lower risk and higher value than a
  broad redesign because it already satisfied the first-fold product gate.

## Unknowns / unchanged external limitations

- No deployed HTTPS smoke was run because no `LIVE_URL` was supplied.
- The strict real-data proof remains governed by the prior checkpoint and must
  not be called verified unless its separate strict smoke is green.
- Docker and external submission gates remain unchanged.
